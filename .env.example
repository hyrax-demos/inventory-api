# Copy to .env and fill in for local development.
DB_HOST=
DB_USER=
DB_PASSWORD=
DB_NAME=

# Required secrets (no defaults are shipped).
SECRET_KEY=
WAREHOUSE_API_KEY=
ADMIN_TOKEN=

# Comma-separated allow-list of provider hosts for price sync.
PROVIDER_ALLOWED_HOSTS=prices.warehouse-provider.example
