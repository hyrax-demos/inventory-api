name: PR Checks
on:
  pull_request:
jobs:
  check-pr-labels:
    runs-on: ubuntu-latest
    steps:
      - name: Require labels
        run: |
          echo "::error::Pull request is missing a required label. This PR must have at least one label (bug, enhancement, documentation) before it can be merged."
          exit 1
